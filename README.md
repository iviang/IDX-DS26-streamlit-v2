# IDX-DS26-streamlit

This is the version 2 of the streamlit task of the CA-Price-Prediction DS26 project.
- Primary Model: LGBM-log
- reuses the same `model.pkl` as produced from `06_evaluation.ipynb` as `IDX-DS26-streamlit-v1`
- Changes Include:
  - Address input box
  - LotSize unit option (acres/sqft)
  - Formatting updates
